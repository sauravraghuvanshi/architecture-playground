import type { Config } from "tailwindcss";

// Minimal IDE compatibility stub. All real design tokens live in
// app/globals.css under @theme (Tailwind v4 CSS-first configuration).
const config: Config = {
  darkMode: "class",
  content: [],
  theme: {},
  plugins: [],
};

export default config;
